/*
20. Escreva um programa que leia números inteiros no intervalo [0,50]
    e os armazene em um vetor com 10 posições.
    Preencha um segundo vetor apenas com os números ímpares do primeiro vetor.
    Imprima os dois vetores, 2 elementos por linha.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída (printf, scanf)
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))

int main()  // Função principal onde o programa começa
{
    int vetor1[10];   // Declara o primeiro vetor com 10 posições
    int vetor2[10];   // Declara o segundo vetor (para guardar apenas os ímpares)

    int j = 0;        // Contador que indica quantos números ímpares foram encontrados

    // ------------------ ENTRADA DE DADOS ------------------
    printf("Digite 10 numeros inteiros no intervalo [0,50]:\n");

    for (int i = 0; i < 10; i++)  // Laço que repete 10 vezes (para preencher o vetor1)
    {
        do  // Faz o usuário repetir até digitar um número válido
        {
            printf("Vetor1[%d]: ", i);  // Mostra o índice que está sendo preenchido
            scanf("%d", &vetor1[i]);    // Lê um número inteiro e armazena em vetor1[i]

            // Verifica se o número está fora do intervalo permitido
            if (vetor1[i] < 0 || vetor1[i] > 50)
            {
                printf("Numero invalido! Digite um numero entre 0 e 50.\n");
            }

        } while (vetor1[i] < 0 || vetor1[i] > 50);  // Repete enquanto o número for inválido
    }

    // ------------------ PROCESSAMENTO ------------------
    // Percorre o vetor1 e copia apenas os números ímpares para vetor2
    for (int i = 0; i < 10; i++)
    {
        if (vetor1[i] % 2 != 0)  // Verifica se o número é ímpar (resto da divisão por 2 diferente de 0)
        {
            vetor2[j] = vetor1[i];  // Armazena o número ímpar em vetor2 na posição j
            j++;                    // Incrementa o contador j (quantos ímpares foram encontrados)
        }
    }

    // ------------------ SAÍDA DE DADOS ------------------
    printf("\nVetor 1 (todos os numeros):\n");

    // Exibe os 10 números do primeiro vetor (2 por linha)
    for (int i = 0; i < 10; i++)
    {
        printf("%d\t", vetor1[i]);  // Exibe o valor do vetor seguido de uma tabulação (\t)
        if ((i + 1) % 2 == 0)       // A cada dois elementos, quebra a linha // % 2 == 0 verifica se essa quantidade é múltiplo de 2
        {
            printf("\n");
        }
    }

    printf("\nVetor 2 (apenas impares):\n");

    // Exibe apenas os números ímpares armazenados em vetor2
    for (int i = 0; i < j; i++)
    {
        printf("%d\t", vetor2[i]);  // Exibe o valor ímpar seguido de uma tabulação
        if ((i + 1) % 2 == 0)       // Quebra a linha a cada dois números // % 2 == 0 verifica se essa quantidade é múltiplo de 2
        {
            printf("\n");
        }
    }

    // Exibe a quantidade total de números ímpares encontrados
    printf("\nTotal de impares encontrados: %d\n", j);

    system("pause");  // Pausa a execução do programa (funciona no Windows)
    return 0;         // Retorna 0 indicando que o programa terminou corretamente
}
