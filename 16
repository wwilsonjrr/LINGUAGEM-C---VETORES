/*
16. Faça um programa que leia um vetor de 5 posições para números reais e,
    depois, um código inteiro. Se o código for zero, finalize o programa;
    se for 1, mostre o vetor na ordem direta;
    se for 2, mostre o vetor na ordem inversa.
    Caso o código seja diferente de 1 e 2, escreva uma mensagem informando
    que o código é inválido.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída (printf, scanf)
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))

int main()  // Início da função principal do programa
{
    float vetor[5];  // Cria um vetor com 5 posições para armazenar números reais
    int codigo;      // Variável para armazenar o código digitado pelo usuário

    // ---------------------- ENTRADA DE DADOS ----------------------
    printf("Digite 5 numeros reais:\n");  // Solicita os 5 números ao usuário

    for (int i = 0; i < 5; i++)  // Laço para preencher o vetor
    {
        printf("Vetor[%d]: ", i);  // Exibe o índice atual
        scanf("%f", &vetor[i]);    // Lê o número e armazena no vetor
    }

    printf("\nDigite o codigo (0 = sair, 1 = ordem direta, 2 = ordem inversa): ");
    scanf("%d", &codigo);  // Lê o código que define a ação do programa

    // ---------------------- PROCESSAMENTO ----------------------
    if (codigo == 0)  // Caso o código seja 0
    {
        printf("\nPrograma finalizado.\n");  // Mensagem de encerramento
    }
    else if (codigo == 1)  // Caso o código seja 1 (ordem direta)
    {
        printf("\nVetor em ordem direta:\n");
        for (int i = 0; i < 5; i++)  // Percorre o vetor do início ao fim
        {
            printf("%.2f \n", vetor[i]);  // Mostra cada número com duas casas decimais
        }
    }
    else if (codigo == 2)  // Caso o código seja 2 (ordem inversa)
    {
        printf("\nVetor em ordem inversa:\n");
        for (int i = 4; i >= 0; i--)  // Percorre o vetor de trás para frente
        {
            printf("%.2f \n", vetor[i]);  // Mostra cada número com duas casas decimais
        }
    }
    else  // Caso o código não seja 0, 1 nem 2
    {
        printf("\nCodigo invalido!\n");  // Mensagem de erro
    }

    // ---------------------- FINALIZAÇÃO ----------------------
    system("pause");  // Pausa o programa (útil no Windows)
    return 0;         // Indica que o programa terminou corretamente
}

