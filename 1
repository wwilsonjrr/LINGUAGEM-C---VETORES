/*
1. Faça um programa que possua um vetor denominado A que armazene 6 números inteiros.
   O programa deve executar os seguintes passos:

   (a) Atribua os seguintes valores a esse vetor: 1, 0, 5, -2, -5, 7.
   (b) Armazene em uma variável inteira (simples) a soma entre os valores
       das posições A[0], A[1] e A[5] do vetor e mostre na tela esta soma.
   (c) Modifique o vetor na posição 4, atribuindo a esta posição o valor 100.
   (d) Mostre na tela cada valor do vetor A, um em cada linha.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))

int main()
{
    int A[6];   // Declaração do vetor A com 6 posições
    int soma;   // Variável para armazenar a soma

    // (a) Atribui valores ao vetor
    A[0] = 1;
    A[1] = 0;
    A[2] = 5;
    A[3] = -2;
    A[4] = -5;
    A[5] = 7;

    // (b) Calcula a soma de A[0] + A[1] + A[5]
    soma = A[0] + A[1] + A[5];

    // Mostra o resultado da soma
    printf("Soma dos valores A[0], A[1] e A[5] = %d\n", soma);

    // (c) Modifica o valor da posição 4 (A[4] = 100)
    A[4] = 100;

    // (d) Mostra todos os valores do vetor A, um por linha
    printf("\nValores do vetor A:\n");
    for (int i = 0; i < 6; i++)   // Laço de repetição para percorrer o vetor
    {
        printf("A[%d] = %d\n", i, A[i]);
    }

    system("pause");  // Pausa o programa antes de encerrar (Windows)
    return 0;
}

